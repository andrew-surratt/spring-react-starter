spring:
  profiles:
    active: development
    group:
      development: development
      test: test
  security:
    oauth2:
      client:
        registration:
          github:
            clientId: github-client-id
            clientSecret: github-client-secret
